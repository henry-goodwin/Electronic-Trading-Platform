package com.company.Common;

/**
 * These are the commands which will be sent across the network connection.
 */
public enum Command {
    ADD_USER,
    GET_ID_USER,
    GET_USERNAME_USER,
    CHECK_AVAILABILITY,
    DELETE_USER,
    GET_USER_SET
}
